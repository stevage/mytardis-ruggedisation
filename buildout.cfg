[buildout]
parts = django test store staging sphinxbuilder
develop = .
eggs =
    nose
    coverage==3.4
    django-nose
    nosexcover
    MyTARDIS

find-links =
    https://github.com/russell/python-magic/tarball/master#egg=python-magic-0.4.0dev

versions = versions

[versions]
lxml = 2.2.7
python-magic = 0.4.0dev

[django]
recipe = djangorecipe
version = 1.3
project = tardis
projectegg = tardis
settings = settings
eggs = ${buildout:eggs}
wsgi = true

[test]
recipe = djangorecipe
version = 1.3
project = tardis
projectegg = tardis
settings = test_settings
eggs = ${buildout:eggs}

[store]
recipe = z3c.recipe.mkdir
paths = var/store

[staging]
recipe = z3c.recipe.mkdir
paths = var/staging

[sphinxbuilder]
recipe = collective.recipe.sphinxbuilder
source = ${buildout:directory}/docs
build = ${buildout:directory}/docs
eggs = ${buildout:eggs}
extra-paths = ${django:location}
